 apiVersion: v1
 kind: Service
 metadata:
   name: my-service
   annotations:
     testme : testme
 spec:
   selector:
     app.kubernetes.io/name: MyApp
   ports:
    - protocol: TCP
      port: 80
      targetPort: 100
   type: LoadBalancer
